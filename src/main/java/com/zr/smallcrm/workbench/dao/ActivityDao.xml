<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zr.smallcrm.workbench.dao.ActivityDao">

    <sql id="catalog">
        id,
        owner,
        name,
        startDate,
        endDate,
        cost,
        description,
        createTime,
        createBy
    </sql>

    <insert id="saveActivity">
        insert into activity(
        <include refid="catalog"/>
        )values (
        #{id},
        #{owner},
        #{name},
        #{startDate},
        #{endDate},
        #{cost},
        #{description},
        #{createTime},
        #{createBy}
        )
    </insert>
    <update id="updateActivity">
        update activity set
          owner = #{owner},
          name = #{name},
          startDate = #{startDate},
          endDate = #{endDate},
          cost = #{cost},
          description = #{description},
          editTime = #{editTime},
          editBy = #{editBy}
        where id =#{id}
    </update>


    <delete id="delete">
        delete from activity where id in
        <foreach collection="array" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <select id="getList" resultType="com.zr.smallcrm.workbench.model.Activity">
        select a.id ,a.name ,u.name as owner , a.startDate ,a.endDate
        from activity a join user u on a.owner = u.id
        <where>
            <if test="name!=null and name !=''">
                a.name like '%' #{name} '%'
            </if>
            <if test="owner !=null and owner!=''">
                and u.name like '%' #{owner} '%'
            </if>
            <if test="startDate !=null and startDate !=''">
                and startDate &gt;= #{startDate}
            </if>
            <if test="endDate!= null and endDate !=''">
                and endDate &lt;= #{endDate}
            </if>

        </where>
        order by a.createTime desc
    </select>

    <select id="getActivity" resultType="com.zr.smallcrm.workbench.model.Activity">
        select * from activity WHERE id = #{id}
    </select>

    <select id="detail" resultType="com.zr.smallcrm.workbench.model.Activity">
        select
        a.id ,a.name ,u.name as owner , a.startDate ,a.endDate ,a.cost ,a.description ,a.createTime ,a.createBy ,a.editTime ,a.editBy
        from activity a
        join user u
        on a.owner = u.id
        where a.id=#{id}
    </select>


</mapper>